version: '3.8'

services:
  welltrack:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - welltrack-data:/data
    environment:
      - FLASK_ENV=development
      - DB_NAME=/data/welltrack.db
    restart: unless-stopped

volumes:
  welltrack-data:
